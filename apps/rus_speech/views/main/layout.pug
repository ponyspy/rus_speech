doctype html
html
	head
		include header
	body
		.main_block
			.title_block
				if (session && session.user_id)
					.stargazer_block
							a(href='/auth') stargazer
							span= ' | '
							a(href='/auth/logout') выход
			.content_block
				block content
			.footer_block
				.language
					a(href='/lang/ru', class=locale == 'ru' ? 'active': ''): span RUS
					a(href='/lang/en', class=locale == 'en' ? 'active': ''): span ENG
		include counters